name : Branch head Prerelease Images

on:
  push:
    branches:
      - main
      - release/v[0-9]+.0

permissions:
  contents: write
  id-token: write

jobs:
  call-workflow:
    uses: rancher/shell/.github/workflows/head-build.yml@automation-core
    secrets: inherit
